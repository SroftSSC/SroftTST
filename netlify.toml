[build]
  publish = "."
  functions = "functions"

[dev]
  command = "npm run start"
  port = 8888
  functions = "functions"

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
